import { compDetailLoader, compDetailMeta } from "@/lib/seo/loaders"

export const loader = compDetailLoader
export const meta = compDetailMeta

export { CompDetailPage as default } from "@/pages/CompDetailPage"
