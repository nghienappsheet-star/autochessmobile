import { heroDetailLoader, heroDetailMeta } from "@/lib/seo/loaders"

export const loader = heroDetailLoader
export const meta = heroDetailMeta

export { HeroDetailPage as default } from "@/pages/HeroDetailPage"
